import s from './AmazonComponent.module.css';

const AmazonComponent = () => {
    return (
        <div className={s.container}>
            <h1 className={s.header}>Amazon</h1>
            <p className={s.paragraph}>Work hard. Have fun. Make history.</p>
        </div>
    );
};

export default AmazonComponent;
